apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cmp-keycloak-pvc
  namespace: cmp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
