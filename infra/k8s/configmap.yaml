apiVersion: v1
kind: ConfigMap
metadata:
  name: cmp-backend-config
  namespace: cmp
data:
  NODE_ENV: "production"
  PORT: "3000"
  DATABASE_PATH: "/data/cmp.db"
  KEYCLOAK_URL: "https://keycloak.fcitcmp.com"
  KEYCLOAK_REALM: "cmp"
  KEYCLOAK_CLIENT_ID: "cmp-app"
  SMTP_HOST: "mailhog"
  SMTP_PORT: "1025"
  SMTP_FROM: "noreply@fcit-cmp.local"
